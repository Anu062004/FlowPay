{
  "name": "flowpay-blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:all": "hardhat run scripts/deployAll.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
